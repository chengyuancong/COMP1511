//Write a program that prints out whether an integer has
//1 digit
//2 digits
//more ethan 2 digits
//Get it working for positive ints and then extend it to work for negative ints
#include <stdio.h>

int main(void) {
    int x;
    printf("Enter x: ");
    scanf("%d", &x);

    printf("%d has ", x);
   

    return 0;
}